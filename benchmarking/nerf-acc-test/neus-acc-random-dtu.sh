output_dir='outputs'
method_name='active-nerf-acc'

exp_name='nerf-acc-entropy-topk-xgrid-mask-scan24'
timestamp='2023-10-08_001028'
config_path="${output_dir}/${exp_name}/${method_name}/${timestamp}/config.yml"
ns-eval --load-config="${config_path}" \
        --output-path="${output_dir}/${exp_name}/${method_name}/${timestamp}/output.json"

exp_name='nerf-acc-entropy-topk-xgrid-mask-scan37'
timestamp='2023-10-08_004952'
config_path="${output_dir}/${exp_name}/${method_name}/${timestamp}/config.yml"
ns-eval --load-config="${config_path}" \
        --output-path="${output_dir}/${exp_name}/${method_name}/${timestamp}/output.json"

exp_name='nerf-acc-entropy-topk-xgrid-mask-scan40'
timestamp='2023-10-08_012718'
config_path="${output_dir}/${exp_name}/${method_name}/${timestamp}/config.yml"
ns-eval --load-config="${config_path}" \
        --output-path="${output_dir}/${exp_name}/${method_name}/${timestamp}/output.json"

exp_name='nerf-acc-entropy-topk-xgrid-mask-scan55'
timestamp='2023-10-08_020846'
config_path="${output_dir}/${exp_name}/${method_name}/${timestamp}/config.yml"
ns-eval --load-config="${config_path}" \
        --output-path="${output_dir}/${exp_name}/${method_name}/${timestamp}/output.json"

exp_name='nerf-acc-entropy-topk-xgrid-mask-scan63'
timestamp='2023-10-08_024442'
config_path="${output_dir}/${exp_name}/${method_name}/${timestamp}/config.yml"
ns-eval --load-config="${config_path}" \
        --output-path="${output_dir}/${exp_name}/${method_name}/${timestamp}/output.json"

exp_name='nerf-acc-entropy-topk-xgrid-mask-scan65'
timestamp='2023-10-08_031604'
config_path="${output_dir}/${exp_name}/${method_name}/${timestamp}/config.yml"
ns-eval --load-config="${config_path}" \
        --output-path="${output_dir}/${exp_name}/${method_name}/${timestamp}/output.json"

exp_name='nerf-acc-entropy-topk-xgrid-mask-scan69'
timestamp='2023-10-08_034948'
config_path="${output_dir}/${exp_name}/${method_name}/${timestamp}/config.yml"
ns-eval --load-config="${config_path}" \
        --output-path="${output_dir}/${exp_name}/${method_name}/${timestamp}/output.json"

exp_name='nerf-acc-entropy-topk-xgrid-mask-scan83'
timestamp='2023-10-08_042423'
config_path="${output_dir}/${exp_name}/${method_name}/${timestamp}/config.yml"
ns-eval --load-config="${config_path}" \
        --output-path="${output_dir}/${exp_name}/${method_name}/${timestamp}/output.json"

exp_name='nerf-acc-entropy-topk-xgrid-mask-scan97'
timestamp='2023-10-08_045431'
config_path="${output_dir}/${exp_name}/${method_name}/${timestamp}/config.yml"
ns-eval --load-config="${config_path}" \
        --output-path="${output_dir}/${exp_name}/${method_name}/${timestamp}/output.json"

exp_name='nerf-acc-entropy-topk-xgrid-mask-scan105'
timestamp='2023-10-08_052520'
config_path="${output_dir}/${exp_name}/${method_name}/${timestamp}/config.yml"
ns-eval --load-config="${config_path}" \
        --output-path="${output_dir}/${exp_name}/${method_name}/${timestamp}/output.json"

exp_name='nerf-acc-entropy-topk-xgrid-mask-scan106'
timestamp='2023-10-08_060550'
config_path="${output_dir}/${exp_name}/${method_name}/${timestamp}/config.yml"
ns-eval --load-config="${config_path}" \
        --output-path="${output_dir}/${exp_name}/${method_name}/${timestamp}/output.json"

exp_name='nerf-acc-entropy-topk-xgrid-mask-scan110'
timestamp='2023-10-08_064022'
config_path="${output_dir}/${exp_name}/${method_name}/${timestamp}/config.yml"
ns-eval --load-config="${config_path}" \
        --output-path="${output_dir}/${exp_name}/${method_name}/${timestamp}/output.json"

exp_name='nerf-acc-entropy-topk-xgrid-mask-scan114'
timestamp='2023-10-08_071129'
config_path="${output_dir}/${exp_name}/${method_name}/${timestamp}/config.yml"
ns-eval --load-config="${config_path}" \
        --output-path="${output_dir}/${exp_name}/${method_name}/${timestamp}/output.json"

exp_name='nerf-acc-entropy-topk-xgrid-mask-scan118'
timestamp='2023-10-08_074511'
config_path="${output_dir}/${exp_name}/${method_name}/${timestamp}/config.yml"
ns-eval --load-config="${config_path}" \
        --output-path="${output_dir}/${exp_name}/${method_name}/${timestamp}/output.json"

exp_name='nerf-acc-entropy-topk-xgrid-mask-scan122'
timestamp='2023-10-08_081844'
config_path="${output_dir}/${exp_name}/${method_name}/${timestamp}/config.yml"
ns-eval --load-config="${config_path}" \
        --output-path="${output_dir}/${exp_name}/${method_name}/${timestamp}/output.json"